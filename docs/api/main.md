# Main Module

The main module provides the entry point for the application.

::: encryptocli.main.main

The `main()` function serves as the legacy entry point that launches the TUI handler. For new code, consider using the services directly or the interfaces module.
